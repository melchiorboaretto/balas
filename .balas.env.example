# Copy this file to ".balas.env" in the repo root and adjust only the values
# that differ on your machine. Existing environment variables always override
# this file.

# Serial device used by automator/profiler.
# BALAS_SERIAL_PORT=/dev/ttyACM0

# Build/deploy target. Defaults to nxp to preserve the original workflow.
# Use stm32 for NUCLEO-H723ZG or nordic for nRF52840-DK.
# BALAS_TARGET=nxp

# Optional: absolute path to stm32tflm. If omitted, the scripts try:
# 1. "stm32tflm" from PATH
# 2. common ST install locations under $HOME
# BALAS_STM32TFLM_BIN=$HOME/opt/st/x-cube-ai/10.2.0/stedgeai-linux-10.2.0/Utilities/linux/stm32tflm

# Optional: MCUXpresso IDE binary. Default is the usual Linux install path.
# MCUXPRESSO_IDE_BIN=/usr/local/mcuxpressoide/ide/mcuxpressoide

# Optional: workspace directory for the MCUXpresso headless build.
# Default: <repo>/cpp-project
# MCUX_WORKSPACE_DIR=$PWD/cpp-project

# Optional: automatically import the project before the headless build.
# Default: 1
# MCUX_IMPORT_PROJECT=1

# Optional: build configuration used by compile.sh.
# Default: Debug
# BUILD_CONFIG=Debug

# Optional: STM32 CMake project generated for the NUCLEO-H723ZG port.
# STM32_PROJECT_DIR=$PWD/cpp-project/stm32-tflite-test
# STM32_BUILD_DIR=$PWD/cpp-project/stm32-tflite-test/build
# BALAS_STM32_ENABLE_MODEL=OFF
# BALAS_STM32_MODEL_BOOT_MARKER=OFF
# STM32_FIRMWARE_FILE=$PWD/cpp-project/stm32-tflite-test/build/stm32-tflite-test.elf
# STM32_PROGRAMMER_CLI=STM32_Programmer_CLI

# Optional: Nordic nRF52840-DK target using nRF Connect SDK/Zephyr.
# NORDIC_PROJECT_DIR=$PWD/cpp-project/nrf52840-tflite-test
# NORDIC_BUILD_DIR=$PWD/cpp-project/nrf52840-tflite-test/build
# NORDIC_BOARD=nrf52840dk/nrf52840
# NORDIC_NCS_VERSION=v3.4.0
# NORDIC_NCS_DIR=$HOME/ncs/v3.4.0
# NORDIC_TFLM_MODULE_DIR=$HOME/ncs/v3.4.0/optional/modules/lib/tflite-micro
# BALAS_NORDIC_ENABLE_MODEL=OFF
# NORDIC_FIRMWARE_FILE=$PWD/cpp-project/nrf52840-tflite-test/build/zephyr/zephyr.hex
# NORDIC_PROBE_SERIAL=
# NRFUTIL_BIN=nrfutil

# Optional: LinkServer root used by deploy.sh.
# Default: /usr/local/LinkServer
# LINKSERVER_ROOT=/usr/local/LinkServer

# Optional: probe serial for deploy.sh.
# PROBE_SERIAL=
